# Optional. Leave blank to use the built-in Vite dev proxy (/api/yahoo).
# If you deploy the frontend without the dev server, point the app at a
# CORS proxy / your own backend that implements the /api/yahoo endpoints.
#
# VITE_CORS_PROXY=https://corsproxy.io/?

# Optional provider keys can also be entered in the in-app Settings panel.
# FMP_API_KEY=
# FINNHUB_API_KEY=
